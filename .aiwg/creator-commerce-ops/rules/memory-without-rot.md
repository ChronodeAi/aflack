# Rule: memory-without-rot

All learned content patterns must be deduped, evidence-bound, and temporally valid.

- Dedup by stable content hash.
- Reinforce existing insights instead of duplicating them.
- Retire stale insights with `invalid_at` rather than deleting history.
- Retrieve only active, relevant, high-confidence insights into working context.
