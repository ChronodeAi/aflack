# Skill: insight-distillation

Purpose: convert benchmark observations into deduped, active insights with evidence and confidence.

Implementation:
- `aflack improve-cycle`
- `aflack insights-list --min-confidence 0.5`
- `aflack trace-show <trace_id>`

Outputs:
- `insights` rows,
- `improvement_proposals` rows,
- event traces.
